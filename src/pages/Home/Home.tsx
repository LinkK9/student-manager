import React, { ReactElement } from "react";
import AppHeader from "./AppHeader";

const Home: React.FC = (): ReactElement => {
  return (
    <div>
      <AppHeader />
    </div>
  );
};

export default Home;
